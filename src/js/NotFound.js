import React from "react";
import Helmet from "react-helmet";

const NotFound = () => {
    return (
        <div>
            <Helmet>
                <title>Not found</title>
            </Helmet>
            <p>Not found</p>
        </div>
    );
};

export default NotFound;
